<?xml version="1.0" encoding="utf-8"?>

<component name="SDK::Router" extends="Group">
    <interface>
        <!-- OUTPUTS -->
        <field id="currentRoute" type="assocarray" />

        <!-- FUNCTIONS -->
        <function name="goBack" />
        <function name="navigateTo" />
        <function name="setFocus" />
    </interface>

    <children>
        <Group id="routeContainer" />
    </children>

    <script type="text/brightscript" uri="./Router.bs" />
</component>
